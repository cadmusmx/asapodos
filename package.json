{
  "name": "gaso-saas",
  "version": "1.1.2",
  "private": true,
  "packageManager": "pnpm@10.33.4",
  "scripts": {
    "dev": "turbo dev",
    "dev:main": "pnpm --filter @gaso/main dev",
    "dev:admin": "pnpm --filter @gaso/admin dev",
    "build": "turbo build",
    "build:main": "pnpm --filter @gaso/main build",
    "build:admin": "pnpm --filter @gaso/admin build",
    "lint": "turbo lint",
    "format": "turbo format"
  },
  "resolutions": {
    "rimraf": "^5.0.7",
    "@tiptap/core": "^2.0.0"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=8"
  },
  "devDependencies": {
    "turbo": "^2.10.5"
  }
}
